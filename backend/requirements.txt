fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9
requests>=2.31.0
numpy>=1.26.0
pillow>=10.2.0
shapely>=2.0.3
huggingface_hub>=0.21.0
# Optional: install a CPU/CUDA PyTorch build only when adding a compatible,
# flood-finetuned Prithvi segmentation model. The working optical classifier
# and API do not require torch, torchvision, or transformers.
# Sentinel-2 STAC live imagery fetching
rasterio>=1.3.0
pystac-client>=0.7.0
